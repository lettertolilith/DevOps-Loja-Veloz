# =============================================================================
# IMPORTANTE: ESTE É UM SECRET DE EXEMPLO PARA DEV/STAGING.
# Em produção use External Secrets Operator integrado a um KMS
# (AWS Secrets Manager, GCP Secret Manager ou HashiCorp Vault) — nunca commite
# Secrets em texto claro. Os valores aqui estão em base64 só para ilustrar o formato.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: loja-veloz-secrets
  namespace: loja-veloz
type: Opaque
data:
  # echo -n "app" | base64
  POSTGRES_USER: YXBw
  POSTGRES_PASSWORD: YXBw
  RABBITMQ_USER: Z3Vlc3Q=
  RABBITMQ_PASSWORD: Z3Vlc3Q=
